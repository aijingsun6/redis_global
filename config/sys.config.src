[
  {redis_global,
    [
      {redis, #{host=>"${REDIS_HOST}", port=>${REDIS_PORT}, username=><<"${REDIS_USER}">>, password=><<"${REDIS_PASS}">>, database=>${REDIS_DB}, size => ${REDIS_SIZE}}}
    ]
  }
].